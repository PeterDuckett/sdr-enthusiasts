apiVersion: v1
kind: Service
metadata:
  name: {{ include "readsb-protobuf.fullname" . }}
  labels:
    {{- include "readsb-protobuf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "readsb-protobuf.selectorLabels" . | nindent 4 }}
