apiVersion: v1
kind: Service
metadata:
  name: {{ include "radarbox.fullname" . }}
  labels:
    {{- include "radarbox.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "radarbox.selectorLabels" . | nindent 4 }}
